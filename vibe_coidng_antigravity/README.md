# Futuristic Student Portal (2030 Edition)

## 🚀 Setup Instructions

**Prerequisites**:
- Node.js (v18 or higher)
- PostgreSQL (for Backend, optional if just viewing Frontend)

### 1. Backend Setup (NestJS)
Navigate to the `backend` folder and install dependencies:
```bash
cd backend
npm install
```

Start the backend server:
```bash
npm run start:dev
```
The API will be available at `http://localhost:3000`.

### 2. Frontend Setup (Vite)
Open a **new terminal**, navigate to the `frontend` folder:
```bash
cd frontend
npm install
```

Start the frontend development server:
```bash
npm run dev
```
Open the link shown (usually `http://localhost:5173`) in your browser.

## 🌟 Features
- **Glassmorphism UI**: Modern, translucent aesthetics.
- **Command Palette**: Press `Ctrl + K` to navigate instantly.
- **War Room**: Track upcoming exams with countdowns.
- **Drag & Drop Dashboard**: Customize your view.
- **Event-Driven Backend**: NestJS architecture with EventEmitter.
